Add Logging functionality.

Add more efficient download tracking in youtube playlist downloader.

Make the SwissKnife Icon a bit larger.

Remove the command prompts opening & closing while mkvmerge.exe is being called in the youtube playlist downloader.

Add a line to the code of del_try_permissions.exe that also deletes the mkvtoolnix installer executable.

Use MKVToolNix portable instead of the installer.

Use icalcs command properly and use proper commands to edit PATH variables to make the installation process more "User-Friendly".